import React from 'react';
import { Typography } from '@mui/material';

const BobMatrixTab: React.FC = () => {
  return (
    <div>
      <Typography variant="h5">BOB Matrix</Typography>
      <Typography variant="body1">This is the content for the BOB Matrix tab.</Typography>
    </div>
  );
};

export default BobMatrixTab;
