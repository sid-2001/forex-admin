import React from 'react';
import { Typography } from '@mui/material';

const StpTab: React.FC = () => {
  return (
    <div>
      <Typography variant="h5">STP</Typography>
      <Typography variant="body1">This is the content for the STP tab.</Typography>
    </div>
  );
};

export default StpTab;
